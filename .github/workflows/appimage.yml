name: AppImage

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            python3-pip python3-venv \
            patchelf desktop-file-utils \
            fakeroot strace zsync \
            squashfs-tools \
            libfuse2t64

      - name: Install appimage-builder
        run: |
          python3 -m venv /tmp/builder
          /tmp/builder/bin/pip install --upgrade pip
          /tmp/builder/bin/pip install appimage-builder
          echo "/tmp/builder/bin" >> "$GITHUB_PATH"

      - name: Compute version
        id: version
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            v="${GITHUB_REF#refs/tags/v}"
          else
            v="$(git describe --tags --always --dirty 2>/dev/null || echo dev)"
          fi
          echo "version=${v}" >> "$GITHUB_OUTPUT"
          echo "Building Tomodachis ${v}"

      - name: Build AppImage
        env:
          APP_VERSION: ${{ steps.version.outputs.version }}
          APPIMAGE_EXTRACT_AND_RUN: '1'
        run: |
          appimage-builder --recipe AppImageBuilder.yml --skip-tests

      - name: Show built artifacts
        run: ls -lh ./*.AppImage* || true

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Tomodachis-${{ steps.version.outputs.version }}-AppImage
          path: |
            ./*.AppImage
            ./*.AppImage.zsync

      - name: Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            ./*.AppImage
            ./*.AppImage.zsync
